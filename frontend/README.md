# frontend

## General Info
```
- more complete information can be found here in the [Main README](../README.md)
- to run the front end in local development mode, navigate to ./chatbot/frontend and run `npm run serve` after launching your server ([see](../backend/README.md))
- to run the front end within the backend as static files, navigate to ./chatbot/backend and run `dotnet run --launch-profile Staging`
```